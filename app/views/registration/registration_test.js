'use strict';

describe('rentalQ.registration module', function() {

  beforeEach(module('rentalQ.registration'));

  describe('Registration Controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var RegistrationCtrl = $controller('RegistrationCtrl');
      expect(RegistrationCtrl).toBeDefined();
    }));

  });
});
