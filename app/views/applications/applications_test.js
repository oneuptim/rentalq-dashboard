'use strict';

describe('rentalQ.applications module', function() {

  beforeEach(module('rentalQ.applications'));

  describe('Applications Controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ApplicationsCtrl = $controller('ApplicationsCtrl');
      expect(ApplicationsCtrl).toBeDefined();
    }));

  });
});
