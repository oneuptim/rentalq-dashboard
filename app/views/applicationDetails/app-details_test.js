'use strict';

describe('rentalQ.app-details module', function() {

  beforeEach(module('rentalQ.app-details'));

  describe('App Details Controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var AppDetailsCtrl = $controller('AppDetailsCtrl');
      expect(AppDetailsCtrl).toBeDefined();
    }));

  });
});
